# Some code borrowed from Luke Smith (https://github.com/LukeSmithxyz/voidrice/blob/master/.config/zsh/.zshrc)

# Import colorscheme from 'wal' asynchronously
# &   # Run the process in the background.
# ( ) # Hide shell job control messages.
(cat ~/.cache/wal/sequences &)

# To add support for TTYs this line can be optionally added.
source ~/.cache/wal/colors-tty.sh

# Enable colors and change prompt:
autoload -U colors && colors
PS1="%B%{$fg[red]%}[%{$fg[yellow]%}%n%{$fg[green]%}@%{$fg[blue]%}%M %{$fg[magenta]%}%~%{$fg[red]%}]%{$reset_color%}$%b "

# History in cache directory:
HISTSIZE=10000
SAVEHIST=10000
HISTFILE=~/.cache/zsh/history

# Basic auto/tab complete:
autoload -U compinit
zstyle ':completion:*' menu select
zmodload zsh/complist
compinit
_comp_options+=(globdots)		# Include hidden files.

# vi mode
bindkey -v
export KEYTIMEOUT=1

# Use vim keys in tab complete menu:
bindkey -M menuselect 'h' vi-backward-char
bindkey -M menuselect 'k' vi-up-line-or-history
bindkey -M menuselect 'l' vi-forward-char
bindkey -M menuselect 'j' vi-down-line-or-history
bindkey -v '^?' backward-delete-char

# Change cursor shape for different vi modes.
function zle-keymap-select {
  if [[ ${KEYMAP} == vicmd ]] ||
     [[ $1 = 'block' ]]; then
    echo -ne '\e[1 q'
  elif [[ ${KEYMAP} == main ]] ||
       [[ ${KEYMAP} == viins ]] ||
       [[ ${KEYMAP} = '' ]] ||
       [[ $1 = 'beam' ]]; then
    echo -ne '\e[5 q'
  fi
}
zle -N zle-keymap-select
zle-line-init() {
    zle -K viins # initiate `vi insert` as keymap (can be removed if `bindkey -V` has been set elsewhere)
    echo -ne "\e[5 q"
}
zle -N zle-line-init
echo -ne '\e[5 q' # Use beam shape cursor on startup.
preexec() { echo -ne '\e[5 q' ;} # Use beam shape cursor for each new prompt.

# Use lf to switch directories and bind it to ctrl-o
lfcd () {
    tmp="$(mktemp)"
    lf -last-dir-path="$tmp" "$@"
    if [ -f "$tmp" ]; then
        dir="$(cat "$tmp")"
        rm -f "$tmp"
        [ -d "$dir" ] && [ "$dir" != "$(pwd)" ] && cd "$dir"
    fi
}
bindkey -s '^o' 'lfcd\n'

# Print stuff on zsh start
neofetch

# Aliases
alias vim="nvim"
alias v="nvim"
alias l="lf"
alias tr="transmission-remote"

alias gh="cd ~"
alias gm="cd ~/media"
alias gp="cd ~/media/Pictures"
alias gpw="cd ~/media/Pictures/Wallpapers"
alias gdl="cd ~/media/Downloads"
alias gd="cd ~/media/Documents"
alias gm="cd ~/media/Music"
alias gv="cd ~/media/Videos"
alias gc="cd ~/.config"
alias gdev="cd ~/dev"

alias pins="sudo pacman -S"
alias psea="sudo pacman -Ss"
alias pupd="sudo pacman -Syu"
alias prem="sudo pacman -r"

alias abook="abook -C ~/.config/abook/abookrc --datafile ~/.config/abook/addressbook"
alias mbsync="mbsync -c $XDG_CONFIG_HOME/isync/mbsyncrc"
alias wget="wget --hsts-file='$XDG_CACHE_HOME/wget-hsts'"
alias startx="startx "$XDG_CONFIG_HOME/X11/xinitrc" vt1"

# Load zsh-syntax-highlighting; should be last.
source /usr/share/zsh/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh 2>/dev/null

